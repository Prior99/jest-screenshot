# jest-screenshot example

This is a demonstration on how to use [jest-screenshot](..) with [puppeteer](https://github.com/GoogleChrome/puppeteer).

In order to execute the tests, simply run:

```
yarn
yarn test
```
